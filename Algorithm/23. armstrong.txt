// Check whether a number is armstrong or not

algorithm armstrong():
    read num
    temporary = num
    amg = 0

    while(num > 0):
        unitdigit = (num % 10)                        // Stores the last digit of num
        amg = amg + (unitdigit * unitdigit * unitdigit) // Adds the cube of unitdigit to ang 
        num = floor(num / 10)                             // Floor divide number by 10
    
    if (amg == temporary):
        print "The number is an armstrong number!"
    else:
        print "The number is not an armstrong number!"



    

